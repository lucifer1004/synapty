#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-08-09-026"
title = "feat(cli): synapty wait — event-driven cross-agent sync"
status = "done"
created = "2026-08-09"
started = "2026-08-10"
completed = "2026-08-10"
refs = ["RFC-0004"]
depends_on = ["WI-2026-08-09-024"]

[content]
description = "Implement the RFC-0004 wait primitive: CLI verb, hub-side filtered subscription serving, pinned registration-generation semantics, stable exit codes; teach it in the skill package as the hand-off primitive."

[[content.acceptance_criteria]]
text = "synapty wait --agent <id> --until working|waiting|done|idle [--timeout] served event-driven from the hub stream, no polling [C-WAIT]"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "pinned generation — wait fails (exit 4) when the observed generation ends; same-generation re-register does not disturb; exit codes 0/2/3/4 stable [C-WAIT]"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "skill package teaches wait for hand-offs in place of recv-polling loops (SKILL.md router + references/messaging.md); reinstalled to three harnesses"
status = "done"
category = "chore"

[[content.acceptance_criteria]]
text = "parse tests + hub wait tests (already-satisfied, timeout, unregister-fail, unknown-target rejected); zig build test green"
status = "done"
category = "chore"
