#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-08-05-016"
title = "fix(ssh): remote backspace over SSH — terminfo deploy and TERM fallback"
status = "done"
created = "2026-08-05"
started = "2026-08-05"
completed = "2026-08-05"

[content]
description = "Remote shells with TERM=xterm-ghostty printed spaces instead of backspace (ghostty#5818). Deploy ghostty terminfo to ~/.terminfo on connect, and fall back to TERM=xterm-256color via a generic infocmp -x probe when the active TERM is not resolvable remotely."

[[content.acceptance_criteria]]
text = "ghostty terminfo deployed to ~/.terminfo so TERM=xterm-ghostty resolves remotely"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "fallback to TERM=xterm-256color when the active TERM lacks remote terminfo"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "TERM preamble probes actual TERM generically via infocmp -x"
status = "done"
category = "changed"
