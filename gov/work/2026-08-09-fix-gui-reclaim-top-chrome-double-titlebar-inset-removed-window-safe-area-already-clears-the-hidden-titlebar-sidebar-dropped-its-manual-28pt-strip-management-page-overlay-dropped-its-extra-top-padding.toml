#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-08-09-011"
title = "fix(gui): reclaim top chrome — double titlebar inset removed (window safe area already clears the hidden titlebar; sidebar dropped its manual 28pt strip, management-page overlay dropped its extra top padding)"
status = "done"
created = "2026-08-09"
started = "2026-08-09"
completed = "2026-08-09"

[content]
description = """
The window's top strip was inset twice. The sidebar drew a manual 28pt strip and the management-page overlay added its own top padding, both compensating for a hidden titlebar that the window's safe area already clears — so the compensation was pure loss.

Removing both reclaimed the chrome: measured 68pt to 42pt, and every management page sits about 28pt higher.

Landed with [[WI-2026-08-09-012]] (pane layout presets) and verified together by screenshot against live PTYs."""

[[content.acceptance_criteria]]
text = "management-page overlay drops .padding(.top, titlebarInset); sidebar drops its manual titlebar Color.clear strip — content clears the traffic lights via the window safe area alone"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "screenshot verification at XL — page title and sidebar nav sit ~28pt higher, no traffic-light overlap, window dragging in the top band still works (manual check note)"
status = "done"
category = "chore"
