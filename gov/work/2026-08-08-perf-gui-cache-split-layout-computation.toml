#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-08-08-051"
title = "perf(gui): cache split layout computation"
status = "done"
created = "2026-08-08"
started = "2026-08-08"
completed = "2026-08-08"

[content]
description = "Audit item #5: AllPanesSplitView recomputes split frames/dividers and flattens allLeaves on every body evaluation. Add a size-keyed cache for computeFrames/computeDividers results and cache allLeaves per publish (invalidation on session/pane/split changes or size change)."

[[content.acceptance_criteria]]
text = "fix: frame/divider computation is cached across bodies for unchanged size/tree"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "fix: allLeaves flattening is cached (not per body)"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "test: 128 Swift tests still green"
status = "done"
category = "chore"
