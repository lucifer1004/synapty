#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-03-26-006"
title = "GUI app shell with single terminal pane"
status = "done"
created = "2026-03-26"
started = "2026-03-26"
completed = "2026-03-26"
refs = ["ADR-0001"]

[content]
description = "Create Xcode project with SwiftUI shell, link GhosttyKit xcframework, and render a single working terminal pane (ghostty_surface_t) running a local shell. Follows cmux pattern: Swift UI + Zig core via C bridging header."

notes = [
    "2026-03-26 (gui): Synapty.app builds and launches. Single terminal pane renders via GhosttyKit with Metal. Keyboard input working via NSTextInputClient protocol. Required ghostty_init() before config creation, NSTextInputClient for proper text input, -lc++ and system frameworks for linking, embed:false for static xcframework.",
]
[[content.acceptance_criteria]]
text = "Xcode project builds and launches on macOS"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "Single terminal pane renders a local shell with full VT emulation"
status = "done"
category = "added"

[[content.acceptance_criteria]]
text = "ghostty.h bridging header imported"
status = "done"
category = "added"
