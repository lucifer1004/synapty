#:schema ../schema/work.schema.json

[govctl]
id = "WI-2026-08-09-013"
title = "fix(gui): multi-tab tab bar polish — active tab adopts the DS.selection pill grammar (was white-on-white raised chip), inactive tabs get stable geometry (always-reserved close slot), per-tab glyph noise removed, chip metrics scaled"
status = "done"
created = "2026-08-09"
started = "2026-08-09"
completed = "2026-08-09"

[content]
description = "Multi-tab audit (4-tab screenshot, XL): active tab was a white raised chip on the WI-010 white surface band — only a hairline border and bold text distinguished it; inactive tabs were bare floating labels with no clickable affordance; the close button appeared on hover and shifted tab width (tab dance); chip height 24/glyph 8/close 8 were fixed while text scales; every tab repeated the same </> glyph. Fix: active tab uses the same DS.selection pill as sidebar nav and session rows (ONE selection grammar for navigation surfaces — accent fill stays reserved for DSSegmented controls); close slot always reserved (opacity+hitTesting gate); glyph dropped; all chip metrics DS.scaled."

[[content.acceptance_criteria]]
text = "active tab = DS.selection pill + bold text (no border/shadow white chip); inactive = transparent with hover fill; close button space always reserved so hover never shifts widths"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "per-tab </> glyph removed (PaneTab + PlaceholderTab); chip height/close/gaps DS.scaled"
status = "done"
category = "fixed"

[[content.acceptance_criteria]]
text = "build+suite green; 4-tab XL screenshot — active pill clearly distinct, stable widths"
status = "done"
category = "chore"
